export default class formVal {
    constructor(form={},rules=[],msg=[]) {
        this.form = form
        this.rules = rules
        this.msg = msg
    }
    
    formValor() {
        for(let item in this.rules) {
            
        }
    }
    
}